.roar/
.venv/
__pycache__/
*.pyc
cast/*.tmp

# Pipeline outputs — all derived. Reproduction regenerates them from code.
data/
model.pkl
metrics.json

# Pre-bake output — regenerated each time, keeps the tree clean for roar run.
ACT1_HASH.txt
